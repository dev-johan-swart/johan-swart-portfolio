# Secure Login Demo (Express + SQLite + Sessions)

## Quick start (development)
1. Clone or copy the project.
2. Create `.env` in `server/` based on `.env.example`:
   - `SESSION_SECRET` must be a long random string.
3. Install server deps:
   ```bash
   cd server
   npm install
